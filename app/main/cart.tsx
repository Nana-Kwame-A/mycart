import { View, Text } from 'react-native'
import React from 'react'
import ScreenHeader from '../../components/ScreenHeader'

export default function cart() {
  return (
    <View>
      <ScreenHeader name = "My Cart" />
    </View>
  )
}