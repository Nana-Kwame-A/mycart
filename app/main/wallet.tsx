import { View, Text } from 'react-native'
import React from 'react'
import ScreenHeader from '../../components/ScreenHeader';

export default function wallet() {
  return (
    <View>
      <ScreenHeader name="My E-Wallet" />
    </View>
  );
}